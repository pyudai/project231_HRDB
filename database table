BEGIN
    BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Familymember';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Familymember (ChildID char(6) DEFAULT NULL,StaffID char(6) DEFAULT NULL,ChildName varchar(100) DEFAULT NULL,ChildDOB DATE DEFAULT NULL)';
    BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Vacation';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Vacation (VacationNo number(6) DEFAULT NULL,StaffID char(6) DEFAULT NULL,VDateStart DATE DEFAULT NULL,VDateEnd DATE DEFAULT NULL,No number(2) DEFAULT NULL)'; 
    BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE MonthlyPaySlip';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE MonthlyPaySlip (PayrollNo char(6) DEFAULT NULL,VacationN0 number(6) DEFAULT NULL,AbsentNo number(6) DEFAULT NULL,StaffID char(6) DEFAULT NULL,PositionID char(3) DEFAULT NULL,DepartmentID char(3) DEFAULT NULL,PayDate DATE DEFAULT NULL,TCDate DATE DEFAULT NULL)';
    BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE EmployeeInfo';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE EmployeeInfo (StaffID char(6) DEFAULT NULL,F_Name varchar(20) DEFAULT NULL,L_Name varchar(50) DEFAULT NULL,DOB DATE DEFAULT NULL,Email varchar(30) DEFAULT NULL,MaritalStatus char(1) DEFAULT NULL,CitizenID char(13) DEFAULT NULL,TelephoneNo char(10) DEFAULT NULL,TaxID char(13) DEFAULT NULL,StartDate DATE DEFAULT NULL,AccountNo varchar(13) DEFAULT NULL,InsuranceID char(13) DEFAULT NULL,Gender char(1) DEFAULT NULL,Address varchar(100) DEFAULT NULL)';
    BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Absent';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Absent (AbsentNo number(6) DEFAULT NULL,StaffID char(6) DEFAULT NULL,AbsentDate DATE DEFAULT NULL)';

 BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Welfare';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Welfare (StaffID char(6) DEFAULT NULL,BenefitCode char(4) DEFAULT NULL)';
 BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE EmployeeBenefits';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE EmployeeBenefits (BenefitCode char(4) DEFAULT NULL,BeniefitsDescription varchar(50) DEFAULT NULL)';
 BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE EmployeeDeduction';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE EmployeeDeduction (StaffID char(6) DEFAULT NULL,DeductionCode char(4) DEFAULT NULL,AmountDed number(4) DEFAULT NULL)';
 BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE DeductionCode';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE DeductionCode (DeductionCode char(4) DEFAULT NULL,DescriptionDed varchar(50) DEFAULT NULL)';
 BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Employeeincome';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Employeeincome (StaffID char(4) DEFAULT NULL,IncomeCode char(4) DEFAULT NULL,Amountin number(6) DEFAULT NULL)';


 BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE IncomeCode';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE IncomeCode(IncomeCode char(4) DEFAULT NULL,DescriptionIn varchar(50) DEFAULT NULL)';


 BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE WorkHistory';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE WorkHistory (StaffID char(6) DEFAULT NULL,CompanyName varchar(50) DEFAULT NULL,StartMonth DATE DEFAULT NULL,EndMonth DATE DEFAULT NULL,HistoryPosition varchar(20) DEFAULT NULL,ExitInterview varchar(100) DEFAULT NULL,No number(2) DEFAULT NULL)';
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE PromotionalHistory';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE PromotionalHistory(StaffID char(6) DEFAULT NULL,PositionID char(3) DEFAULT NULL,DepartmentID char(3) DEFAULT NULL,TrainingNo number(6) DEFAULT NULL,PerformanceReviewDate DATE DEFAULT NULL,PromotionDate DATE DEFAULT NULL,No number(2) DEFAULT NULL,SuperVisorName varchar(70) DEFAULT NULL)';
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Department';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Department(DepartmentID char(3) DEFAULT NULL,Department_Name varchar(20) DEFAULT NULL)';
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Position';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Position(PositionID char(3) DEFAULT NULL,PositionName varchar(20) DEFAULT NULL,StartSalary number(6) DEFAULT NULL)';

BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE EducationalHistory';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE EducationalHistory(StaffID char(6) DEFAULT NULL,GraduationDate DATE DEFAULT NULL,DegreeID varchar(10) DEFAULT NULL,Levels varchar(20) DEFAULT NULL)';
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Degree';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Degree(DegreeID varchar(10) DEFAULT NULL,DegreeName varchar(50) DEFAULT NULL)';

BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE DailyTimeCard';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE DailyTimeCard(StaffID char(6) DEFAULT NULL,TCDate DATE DEFAULT NULL,TimeIn TIMESTAMP DEFAULT NULL,TimeOut TIMESTAMP DEFAULT NULL)';
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE EmployeeSkill';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE EmployeeSkill(StaffID char(6) DEFAULT NULL,SkillID char(6) DEFAULT NULL)';
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Course';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Course(CourseID char(3) DEFAULT NULL,CourseName varchar(20) DEFAULT NULL,CourseDescription varchar(100) DEFAULT NULL,StartCourse DATE DEFAULT NULL,EndCourse DATE DEFAULT NULL)';
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Skill';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Skill(SkillID char(6) DEFAULT NULL,SkillName varchar(20) DEFAULT NULL,SkillDescription varchar(50) DEFAULT NULL)';
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE PerformanceReview';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE PerformanceReview(StaffID char(6) DEFAULT NULL,PerformanceReviewDate DATE DEFAULT NULL,JobKnowledge char(1) DEFAULT NULL,QuantityofWork char(1) DEFAULT NULL,QualityofWork char(1) DEFAULT NULL,Comments varchar(100) DEFAULT NULL)';
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE TrainingHistory';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE TrainingHistory(TrainingNo number(6) DEFAULT NULL,StaffID char(6) DEFAULT NULL,CourseID char(3) DEFAULT NULL,SkillID char(6) DEFAULT NULL,THDate DATE DEFAULT NULL,No number(2) DEFAULT NULL,Result number(2) DEFAULT NULL)';
END;