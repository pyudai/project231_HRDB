BEGIN
    BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Familymember';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Familymember (ChildID char(6) NOT NULL,StaffID char(6) NOT NULL,ChildName varchar(100) NOT NULL,ChildDOB DATE NOT NULL,
    PRIMARY KEY (ChildID))';
    
    BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Vacation';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Vacation (VacationNo number(6) NOT NULL,StaffID char(6) NOT NULL,VDateStart DATE NOT NULL,VDateEnd DATE NOT NULL,No number(2) NOT NULL,
    PRIMARY KEY (VacationNO))';
    
    BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE MonthlyPaySlip';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE MonthlyPaySlip (PayrollNo char(6) NOT NULL,VacationN0 number(6) NOT NULL,AbsentNo number(6) NOT NULL,StaffID char(6) NOT NULL,PositionID char(3) NOT NULL,DepartmentID char(3) NOT NULL,PayDate DATE NOT NULL,TCDate DATE NOT NULL,
    CONSTRAINT PK_MonthPay PRIMARY KEY (PayrollNo, AbsentNo))';
    
    BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE EmployeeInfo';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE EmployeeInfo (StaffID char(6) NOT NULL,F_Name varchar(20) NOT NULL,L_Name varchar(50) NOT NULL,DOB DATE NOT NULL,Email varchar(30) DEFAULT NULL,MaritalStatus char(1) NOT NULL,CitizenID char(13) NOT NULL,TelephoneNo char(10) NOT NULL,TaxID char(13) NOT NULL,StartDate DATE NOT NULL,AccountNo varchar(13) NOT NULL,InsuranceID char(13) NOT NULL,Gender char(1) NOT NULL,Address varchar(100) NOT NULL,
    PRIMARY KEY (StaffID))';
    
    BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Absent';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Absent (AbsentNo number(6) NOT NULL,StaffID char(6) NOT NULL,AbsentDate DATE NOT NULL,
    PRIMARY KEY (AbsentNo))';

 BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Welfare';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Welfare (StaffID char(6) NOT NULL,BenefitCode char(4) NOT NULL,
    CONSTRAINT PK_welfare PRIMARY KEY (StaffID, BenefitCode))';
    
 BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE EmployeeBenefits';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE EmployeeBenefits (BenefitCode char(4) NOT NULL,BeniefitsDescription varchar(50) NOT NULL,
    PRIMARY KEY (BenefitCode))';
    
 BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE EmployeeDeduction';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE EmployeeDeduction (StaffID char(6) NOT NULL,DeductionCode char(4) NOT NULL,AmountDed number(4) NOT NULL,
    CONSTRAINT PK_EmpDed PRIMARY KEY (StaffID, DeductionCode))';
    
 BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE DeductionCode';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE DeductionCode (DeductionCode char(4) NOT NULL,DescriptionDed varchar(50) NOT NULL,
    PRIMARY KEY (DeductionCode))';
    
 BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE EmployeeIncome';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE EmployeeIncome (StaffID char(4) NOT NULL,IncomeCode char(4) NOT NULL,Amountin number(6) NOT NULL,
    CONSTRAINT PK_EmpInc PRIMARY KEY (StaffID, IncomeCode))';
    
 BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE IncomeCode';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE IncomeCode(IncomeCode char(4) NOT NULL,DescriptionIn varchar(50) NOT NULL,
    PRIMARY KEY (IncomeCode))';

 BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE WorkHistory';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE WorkHistory (StaffID char(6) NOT NULL,CompanyName varchar(50) NOT NULL,StartMonth DATE NOT NULL,EndMonth DATE NOT NULL,HistoryPosition varchar(20) NOT NULL,ExitInterview varchar(100) NOT NULL,No number(2) DEFAULT NULL, 
    CONSTRAINT PK_WorkHis PRIMARY KEY (StaffID, CompanyName, StartMonth))';
    
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE PromotionalHistory';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE PromotionalHistory(StaffID char(6) NOT NULL,PositionID char(3) NOT NULL,DepartmentID char(3) NOT NULL,TrainingNo number(6) NOT NULL,PerformanceReviewDate DATE NOT NULL,PromotionDate DATE NOT NULL,No number(2) NOT NULL,SuperVisorName varchar(70) NOT NULL,
    CONSTRAINT PK_ProHis PRIMARY KEY (StaffID, PositionID))';
    
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Department';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Department(DepartmentID char(3) NOT NULL,Department_Name varchar(20) NOT NULL,
    PRIMARY KEY (DepartmentID))';
    
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Position';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Position(PositionID char(3) NOT NULL,PositionName varchar(20) NOT NULL,StartSalary number(6) NOT NULL,
    PRIMARY KEY (PositionID))';

BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE EducationalHistory';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE EducationalHistory(StaffID char(6) NOT NULL,GraduationDate DATE NOT NULL,DegreeID varchar(10) NOT NULL,Levels varchar(20) NOT NULL,
    CONSTRAINT PK_EduHis PRIMARY KEY (StaffID, GraduationDate, Levels))';
    
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Degree';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Degree(DegreeID varchar(10) NOT NULL,DegreeName varchar(50) NOT NULL,
    PRIMARY KEY (DegreeID))';

BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE DailyTimeCard';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE DailyTimeCard(StaffID char(6) NOT NULL,TCDate DATE NOT NULL,TimeIn TIMESTAMP NOT NULL,TimeOut TIMESTAMP NOT NULL,
    CONSTRAINT PK_DaiTime PRIMARY KEY (StaffID, TCDate))';
    
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE EmployeeSkill';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE EmployeeSkill(StaffID char(6) NOT NULL,SkillID char(6) NOT NULL,
    CONSTRAINT PK_EmpSki PRIMARY KEY (StaffID, SkillID))';
    
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Course';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Course(CourseID char(3) NOT NULL,CourseName varchar(20) NOT NULL,CourseDescription varchar(100) NOT NULL,StartCourse DATE NOT NULL,EndCourse DATE NOT NULL,
    PRIMARY KEY (CourseID))';
    
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE Skill';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE Skill(SkillID char(6) NOT NULL,SkillName varchar(20) NOT NULL,SkillDescription varchar(50) NOT NULL,
    PRIMARY KEY (SkillID))';
    
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE PerformanceReview';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE PerformanceReview(StaffID char(6) NOT NULL,PerformanceReviewDate DATE NOT NULL,JobKnowledge char(1) NOT NULL,QuantityofWork char(1) NOT NULL,QualityofWork char(1) NOT NULL,Comments varchar(100) NOT NULL,
    CONSTRAINT PK_PerRev PRIMARY KEY (StaffID, PerformanceReviewDate))';
    
BEGIN
        EXECUTE IMMEDIATE 'DROP TABLE TrainingHistory';
    EXCEPTION
        WHEN OTHERS THEN
            IF SQLCODE != -942 THEN
                RAISE;
            END IF;
    END;
    EXECUTE IMMEDIATE 'CREATE TABLE TrainingHistory(TrainingNo number(6) NOT NULL,StaffID char(6) NOT NULL,CourseID char(3) NOT NULL,SkillID char(6) NOT NULL,THDate DATE NOT NULL,No number(2) NOT NULL,Result number(2) NOT NULL,
    PRIMARY KEY (TrainingNo))';
END;

